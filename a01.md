# a01: 이 프로젝트의 i18n 구현 정리

## 1. 한 줄 요약

이 프로젝트의 i18n은 **URL path 첫 segment를 locale로 쓰는 방식**이다.

```text
/ko/home  -> Locale.KOREAN  -> messages_ko.properties -> 한국어 HTML
/en/home  -> Locale.ENGLISH -> messages_en.properties -> 영어 HTML
```

즉, 사용자가 어떤 언어 페이지를 요청했는지는 `?lang=en` 같은 query parameter가 아니라 `/ko/...`, `/en/...` 경로로 결정한다.

---

## 2. 사용자 요청에서 구현 방향으로 바뀐 내용

요구사항은 대략 다음 흐름이었다.

1. 한국어/영어 페이지를 제공한다.
2. URL, 서버 locale, HTML `lang`, 화면 문구가 서로 어긋나지 않아야 한다.
3. Thymeleaf 서버 렌더링에서 `#{...}` 메시지가 올바른 언어 번들을 읽어야 한다.
4. SEO용 `canonical`, `hreflang`도 언어별로 맞춰야 한다.
5. 번역 문구는 Excel/CSV로 관리할 수 있어야 한다.
6. i18n이 깨졌는지 테스트로 확인할 수 있어야 한다.

그래서 구현 방향은 다음처럼 잡았다.

```text
사용자 요청
  -> path 기반 locale 정책
  -> LocaleRoutingFilter로 잘못된 locale 경로 정리
  -> PathLocaleResolver로 Spring Locale 결정
  -> RouterController에서 lang/path/page model 구성
  -> SiteContentService가 locale별 메시지 key 조회
  -> Thymeleaf가 MessageSource + model로 HTML 렌더링
  -> 테스트와 CSV 변환 스크립트로 유지보수
```

---

## 3. 런타임 요청 처리 파이프라인

예시 요청: `/en/home`

```text
Browser
  ↓
LocaleRoutingFilter
  ↓
PathLocaleResolver
  ↓
RouterController
  ↓
SiteContentService / TrustedMessageService
  ↓
Thymeleaf templates
  ↓
HTML response
```

### 3.1 `LocaleRoutingFilter`: URL을 먼저 정리한다

파일:

```text
src/main/java/com/talenthub/config/LocaleRoutingFilter.java
```

역할:

- `/` 접근 시 `/ko/home`으로 redirect
- `/fr/home`처럼 미지원 언어 접근 시 `/ko/home`으로 redirect
- `/EN/home`처럼 대문자 locale 접근 시 `/en/home`으로 canonical redirect
- `/ko`, `/ko/`, `/en`, `/en/` 접근 시 각 언어의 `/home`으로 redirect
- `/api`, `/actuator`, `/css`, `/js`, `/robots.txt` 같은 infrastructure path는 i18n page filter에서 제외

핵심 흐름:

```text
request path 확인
  -> infrastructure path면 pass-through
  -> root면 /ko/home redirect
  -> 첫 segment가 ko/en인지 검사
  -> 미지원이면 /ko/home redirect
  -> 대문자면 lowercase path로 redirect
  -> /ko 또는 /en이면 /ko/home 또는 /en/home redirect
  -> 정상 page path면 controller로 전달
```

### 3.2 `PathLocaleResolver`: URL path를 Spring Locale로 바꾼다

파일:

```text
src/main/java/com/talenthub/config/PathLocaleResolver.java
```

역할:

- request URI 첫 path segment를 읽는다.
- `ko`면 `Locale.KOREAN`
- `en`이면 `Locale.ENGLISH`
- 없거나 이상하면 기본값 `Locale.KOREAN`

이 resolver 덕분에 Thymeleaf의 `#{message.key}`가 현재 요청 언어에 맞는 메시지 번들을 읽는다.

### 3.3 `WebConfig`: MessageSource, LocaleResolver, Filter를 등록한다

파일:

```text
src/main/java/com/talenthub/config/WebConfig.java
```

역할:

- `MessageSource` basename을 `classpath:messages`로 설정
- UTF-8 인코딩 설정
- system locale fallback 비활성화
- `localeResolver` bean으로 `PathLocaleResolver` 등록
- `LocaleRoutingFilter`를 최우선 filter로 등록

중요 설정:

```java
messageSource.setBasename("classpath:messages");
messageSource.setDefaultEncoding(StandardCharsets.UTF_8.name());
messageSource.setFallbackToSystemLocale(false);
```

`classpath:messages`로 설정하면 Spring이 locale에 따라 다음 파일을 찾는다.

```text
src/main/resources/messages_ko.properties
src/main/resources/messages_en.properties
```

### 3.4 `LocalePolicy`: i18n 정책을 한 곳에 모은다

파일:

```text
src/main/java/com/talenthub/config/LocalePolicy.java
```

역할:

- 지원 언어: `ko`, `en`
- 기본 locale: `Locale.KOREAN`
- 기본 홈: `/ko/home`
- controller route pattern: `/{lang:ko|en}`
- infrastructure path 목록 관리
- 첫 path segment 추출/언어 normalization 제공

이 파일을 기준으로 filter, resolver, controller가 같은 정책을 공유한다.

### 3.5 `RouterController`: 언어 path가 있는 page route만 받는다

파일:

```text
src/main/java/com/talenthub/web/RouterController.java
```

역할:

- 모든 page route를 `/{lang:ko|en}` 아래에서만 받는다.
- handler method에서 `@PathVariable String lang`과 `Locale locale`을 같이 받는다.
- `addCommon(...)`에서 모든 페이지 공통 model을 구성한다.

중요 구조:

```java
@Controller
@RequestMapping(LocalePolicy.SUPPORTED_LANGUAGE_PATH_PATTERN)
public class RouterController {
    @GetMapping("/home")
    public String home(@PathVariable String lang,
                       HttpServletRequest request,
                       Locale locale,
                       Model model) {
        ...
    }
}
```

`lang`은 URL 표시용/링크 생성용이고, `locale`은 실제 메시지 조회용이다. 정상 경로에서는 둘이 같은 언어를 가리킨다.

`addCommon(...)`에서 넣는 주요 model:

| model key | 의미 |
| --- | --- |
| `seo` | title/description |
| `lang` | `ko` 또는 `en`; `<html lang>`과 link 생성에 사용 |
| `navGroups` | 현재 locale로 번역된 navigation |
| `langSwitchUrls` | 현재 path를 유지한 언어 전환 URL |
| `canonicalUrl` | 현재 언어 canonical URL |
| `alternateUrls` | `hreflang=ko/en/x-default` URL |
| `currentYear` | footer 연도 |

### 3.6 `SiteContentService`: 메시지 key를 화면 DTO로 조립한다

파일:

```text
src/main/java/com/talenthub/service/SiteContentService.java
```

역할:

- 화면에 필요한 카드, SEO, nav, FAQ, 직무 상세, 인터뷰 상세 등을 조립한다.
- 문장 자체는 Java 코드에 직접 쓰지 않고 message key로 조회한다.
- `Locale locale`을 받아 locale별 문구를 가져온다.

예시:

```java
public SeoData seo(String pageKey, Locale locale) {
    return new SeoData(
        text("seo." + pageKey + ".title", locale),
        text("seo." + pageKey + ".description", locale)
    );
}
```

리스트형 메시지는 `|`로 구분해서 작성하고 `messages.list(...)`로 분리한다.

```text
job.roles.ai-platform.skills=AI 서비스 기획|데이터 파이프라인 이해|플랫폼 운영
```

### 3.7 `TrustedMessageService`: 메시지 조회, fallback, HTML sanitizing

파일:

```text
src/main/java/com/talenthub/service/TrustedMessageService.java
```

역할:

- `MessageSource.getMessage(...)`로 locale별 메시지를 조회한다.
- 특정 locale에 key가 없으면 한국어 fallback을 시도한다.
- 그래도 없으면 key 자체를 반환하고 warn log를 남긴다.
- `|`로 된 문자열을 list로 분리한다.
- trusted HTML 문구는 escape 후 허용 태그만 되살린다.

허용 태그:

```text
<br>, <br/>, <strong>, </strong>, <em>, </em>
```

주의:

- `th:utext`는 trusted message에만 사용한다.
- 사용자 입력이나 외부 데이터는 raw HTML로 출력하면 안 된다.

### 3.8 Thymeleaf templates: 메시지와 model을 HTML로 렌더링한다

대표 파일:

```text
src/main/resources/templates/layout/base.html
src/main/resources/templates/fragments/header.html
src/main/resources/templates/fragments/footer.html
src/main/resources/templates/home.html
src/main/resources/templates/company/list.html
src/main/resources/templates/job/*.html
src/main/resources/templates/culture/*.html
src/main/resources/templates/apply/*.html
```

사용 방식:

```html
<html th:lang="${lang}">
<title th:text="${seo.title}">Talent Hub</title>
<a th:text="#{common.skipToContent}">본문으로 건너뛰기</a>
<h1 th:utext="${@trustedMessageService.html('home.hero.titleHtml', #locale)}">Hero</h1>
```

구분:

| 문법 | 사용처 |
| --- | --- |
| `#{key}` | Spring `Locale` 기준 message bundle 조회 |
| `${model}` | controller/service가 넣은 model 값 출력 |
| `#locale` | Thymeleaf에서 현재 Spring locale 참조 |
| `th:lang="${lang}"` | HTML `lang` 속성 지정 |
| `th:utext` | sanitize된 trusted HTML만 출력 |

---

## 4. 메시지 파일과 번역 관리 파이프라인

### 4.1 실제 메시지 번들

파일:

```text
src/main/resources/messages_ko.properties
src/main/resources/messages_en.properties
```

규칙:

- 두 파일은 같은 key set을 가져야 한다.
- `messages.properties` 기본 번들은 사용하지 않는다.
- Java/template에는 긴 문장을 직접 넣지 않고 key만 둔다.
- HTML이 필요한 문구는 key 이름에 `Html`을 붙이는 식으로 구분한다.

예시:

```properties
home.hero.titleHtml=반도체와 디지털 혁신이 만나는 <br/><strong>당신의 다음 커리어</strong>
home.hero.primaryCta=직무 소개 보기
```

```properties
home.hero.titleHtml=Where semiconductor innovation meets <br/><strong>your next career chapter</strong>
home.hero.primaryCta=Explore roles
```

### 4.2 Excel/CSV 번역 워크플로

파일:

```text
scripts/messages-excel.js
i18n/messages.csv
```

흐름:

```text
messages_ko.properties + messages_en.properties
  -> node scripts/messages-excel.js export
  -> i18n/messages.csv
  -> Excel에서 ko/en 컬럼 편집
  -> node scripts/messages-excel.js import
  -> messages_ko.properties + messages_en.properties 재생성
```

명령어:

```bash
node scripts/messages-excel.js export
node scripts/messages-excel.js import
```

CSV 컬럼:

```text
group,key,ko,en,status,notes
```

커스텀 CSV 경로:

```bash
node scripts/messages-excel.js export --csv ./i18n/messages-for-translators.csv
node scripts/messages-excel.js import --csv ./i18n/messages-for-translators.csv
```

---

## 5. 어떤 파일을 건드리면 되는가

### 5.1 i18n infrastructure를 바꿀 때

| 목적 | 수정 파일 |
| --- | --- |
| 지원 언어 추가/삭제 | `src/main/java/com/talenthub/config/LocalePolicy.java` |
| URL locale redirect 규칙 변경 | `src/main/java/com/talenthub/config/LocaleRoutingFilter.java` |
| path가 아닌 다른 기준으로 locale 결정 | `src/main/java/com/talenthub/config/PathLocaleResolver.java` |
| MessageSource basename/encoding/fallback 변경 | `src/main/java/com/talenthub/config/WebConfig.java` |
| infrastructure path 예외 추가 | `src/main/java/com/talenthub/config/LocalePolicy.java` |

### 5.2 새 페이지를 추가할 때

보통 아래 파일을 수정한다.

| 단계 | 수정 파일 | 할 일 |
| --- | --- | --- |
| route 추가 | `src/main/java/com/talenthub/web/RouterController.java` | `@GetMapping` 추가, `addCommon` 호출 |
| content 조립 | `src/main/java/com/talenthub/service/SiteContentService.java` | message key를 DTO로 조립하는 method 추가 |
| DTO 필요 시 | `src/main/java/com/talenthub/model/SiteViewModels.java` | template에 넘길 view model 추가 |
| 화면 작성 | `src/main/resources/templates/**` | `#{key}`와 `${model}`로 렌더링 |
| 문구 추가 | `messages_ko.properties`, `messages_en.properties` | 같은 key를 양쪽에 추가 |
| 테스트 추가 | `src/test/java/com/talenthub/web/RouterControllerTest.java` | route, lang, 주요 문구 검증 |

### 5.3 문구만 수정할 때

둘 중 하나로 한다.

1. properties 직접 수정

```text
src/main/resources/messages_ko.properties
src/main/resources/messages_en.properties
```

2. CSV로 수정

```bash
node scripts/messages-excel.js export
# i18n/messages.csv 편집
node scripts/messages-excel.js import
```

그 다음 반드시 테스트:

```bash
./mvnw test
```

### 5.4 언어를 추가할 때 예: 일본어 `ja`

수정 범위가 넓다.

1. `LocalePolicy.java`
   - `JAPANESE_LANGUAGE = "ja"` 추가
   - `SUPPORTED_LANGUAGE_PATH_PATTERN`에 `ja` 추가
   - `LOCALES_BY_LANGUAGE`에 `Locale.JAPANESE` 추가
2. `src/main/resources/messages_ja.properties` 생성
3. `RouterController.alternateUrls(...)`, `languageSwitchUrls(...)`에 `ja` 추가
4. `templates/fragments/header.html` 언어 전환 UI에 `JA` 추가
5. `templates/layout/base.html`에 `hreflang="ja"` 추가
6. `scripts/messages-excel.js`는 현재 `ko/en` 전용이므로 CSV 컬럼과 import/export 로직 확장
7. `MessageBundleParityTest`를 다국어 parity 검사로 확장
8. `RouterControllerTest`에 `/ja/home` 렌더링/전환 URL 테스트 추가

---

## 6. 설정 파일 체크리스트

### 6.1 Maven 의존성

파일:

```text
pom.xml
```

현재 i18n에 필요한 주요 의존성:

```text
spring-boot-starter-web
spring-boot-starter-thymeleaf
spring-boot-starter-test
```

별도 i18n 라이브러리는 쓰지 않는다. Spring MVC + Spring MessageSource + Thymeleaf 기본 기능으로 구현한다.

### 6.2 Spring 설정

파일:

```text
src/main/resources/application.yml
```

현재 설정:

```yaml
spring:
  thymeleaf:
    cache: false
server:
  shutdown: graceful
```

개발 중 template 변경 반영을 쉽게 하기 위해 Thymeleaf cache를 꺼둔다.

### 6.3 메시지 리소스

파일:

```text
src/main/resources/messages_ko.properties
src/main/resources/messages_en.properties
```

인코딩은 UTF-8로 유지한다.

---

## 7. 테스트/검증 파이프라인

### 7.1 자동 테스트

파일:

```text
src/test/java/com/talenthub/web/RouterControllerTest.java
src/test/java/com/talenthub/web/MessageBundleParityTest.java
src/test/java/com/talenthub/service/TrustedMessageServiceTest.java
```

검증 내용:

- `/` -> `/ko/home` redirect
- `/fr/home` -> `/ko/home` redirect
- `/EN/home` -> `/en/home` redirect
- `/api/home`, `/actuator/home`이 page router에 잡히지 않는지 확인
- `/en/home`에서 `<html lang="en">`과 영어 문구 렌더링 확인
- `/ko/home`에서 `<html lang="ko">`와 한국어 문구 렌더링 확인
- 언어 전환 링크가 현재 상세 path를 유지하는지 확인
- FAQ 페이지 렌더링 확인
- `messages_ko.properties`, `messages_en.properties` key parity 확인
- trusted HTML sanitizer가 허용 태그만 유지하는지 확인

명령어:

```bash
./mvnw test
```

### 7.2 수동 smoke test

서버 실행:

```bash
./mvnw spring-boot:run
```

확인 URL:

```text
http://localhost:8080/ko/home
http://localhost:8080/en/home
http://localhost:8080/
http://localhost:8080/EN/home
http://localhost:8080/fr/home
```

기대 결과:

| URL | 기대 결과 |
| --- | --- |
| `/ko/home` | 한국어 페이지, `<html lang="ko">` |
| `/en/home` | 영어 페이지, `<html lang="en">` |
| `/` | `/ko/home` redirect |
| `/EN/home` | `/en/home` redirect |
| `/fr/home` | `/ko/home` redirect |

---

## 8. 구현할 때 주의할 점

1. `messages.properties` 기본 번들을 만들지 않는다.
   - key 누락을 숨길 수 있다.
   - 현재는 parity test로 `ko/en` key 일치를 보장한다.

2. `LocaleResolver`를 다른 bean으로 override하지 않는다.
   - `PathLocaleResolver`가 아닌 `AcceptHeaderLocaleResolver` 등이 등록되면 `/en/home`인데 한국어가 나오는 문제가 생길 수 있다.

3. URL의 `lang`과 Spring `Locale`을 분리해서 생각한다.
   - `lang`: link, HTML lang, path 생성용
   - `Locale`: MessageSource/Thymeleaf message 조회용
   - 정상 구현에서는 둘이 같은 언어를 가리키게 유지한다.

4. `th:utext`는 최소화한다.
   - trusted message + sanitizer 경로에서만 사용한다.
   - 사용자 입력/외부 데이터는 절대 raw HTML로 출력하지 않는다.

5. 새 key는 반드시 양쪽 언어 파일에 동시에 추가한다.
   - 누락되면 `MessageBundleParityTest`에서 잡히게 해야 한다.

6. `/api`, `/actuator`, 정적 리소스는 page locale routing에 잡히면 안 된다.
   - infrastructure path 추가가 필요하면 `LocalePolicy`에 넣는다.

---

## 9. 빠른 작업 레시피

### 문구 하나 바꾸기

```text
1. messages_ko.properties / messages_en.properties 수정
2. ./mvnw test
3. /ko/home, /en/home에서 화면 확인
```

### 새 CTA 문구 추가하기

```text
1. messages_ko.properties에 key 추가
2. messages_en.properties에 같은 key 추가
3. template에서 #{new.key} 사용
4. ./mvnw test
```

### 새 카드/직무/FAQ 추가하기

```text
1. SiteContentService의 ID 목록에 slug/id 추가
2. messages_ko.properties / messages_en.properties에 필요한 key 추가
3. 필요하면 template 확인
4. RouterControllerTest 또는 관련 테스트 보강
5. ./mvnw test
```

### 새 route 추가하기

```text
1. RouterController에 @GetMapping 추가
2. addCommon(model, request, lang, locale, seo) 호출
3. SiteContentService에 content 조립 method 추가
4. template 파일 추가
5. messages_ko/en에 SEO + 본문 key 추가
6. RouterControllerTest 추가
7. ./mvnw test
```

---

## 10. 현재 정상 동작 판단 기준

현재 구조에서 i18n이 정상이라는 말은 아래가 모두 맞는다는 뜻이다.

```text
/en/home 요청
  -> RouterController lang = en
  -> PathLocaleResolver locale = Locale.ENGLISH
  -> <html lang="en">
  -> #{...} = messages_en.properties
  -> hreflang ko/en URL 제공
```

```text
/ko/home 요청
  -> RouterController lang = ko
  -> PathLocaleResolver locale = Locale.KOREAN
  -> <html lang="ko">
  -> #{...} = messages_ko.properties
  -> hreflang ko/en URL 제공
```

이 기준은 현재 테스트와 smoke check로 확인 가능하다.
